package com.unchunks.echomark.ui.theme

import androidx.compose.ui.graphics.Color

// ライトモード用の色
val PrimaryLight = Color(0xFF0061A4) // メインカラー（濃いブルー）
val OnPrimaryLight = Color(0xFFFFFFFF) // メインカラーの上に乗る文字色（白）
val PrimaryContainerLight = Color(0xFFD1E4FF) // 吹き出し等に使う薄い背景色
val OnPrimaryContainerLight = Color(0xFF001D36) // 薄い背景色の上に乗る文字色

val BackgroundLight = Color(0xFFFDFDFF) // アプリ全体の背景色（真っ白より少し柔らかい白）
val SurfaceLight = Color(0xFFFDFDFF) // カードやリストの背景色
val OnBackgroundLight = Color(0xFF1A1C1E) // 背景の上に乗る文字色（ほぼ黒）
val OutlineLight = Color(0xFF73777F) // テキストフィールドなどの枠線
val OnSurfaceVariantLight = Color(0xFF43474E) // ライトモード用の少し薄いグレー

// ダークモード用の色
val PrimaryDark = Color(0xFF9ECAFF) // メインカラー（明るいブルー）
val OnPrimaryDark = Color(0xFF003258) // メインカラーの上に乗る文字色（濃い紺）
val PrimaryContainerDark = Color(0xFF00497D) // 吹き出し等に使う背景色
val OnPrimaryContainerDark = Color(0xFFD1E4FF) // 背景色の上に乗る文字色

val BackgroundDark = Color(0xFF1A1C1E) // アプリ全体の背景色（ダークグレー）
val SurfaceDark = Color(0xFF1A1C1E) // カードやリストの背景色
val OnBackgroundDark = Color(0xFFE2E2E6) // 背景の上に乗る文字色（明るいグレー）
val OutlineDark = Color(0xFF8C9199) // テキストフィールドなどの枠線
val OnSurfaceVariantDark = Color(0xFFC3C6CF)  // ダークモード用の少し薄い白